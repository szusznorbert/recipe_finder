import 'package:flutter/material.dart';

class BorderStyles {
  static roundedBorder(Color color) => OutlineInputBorder(borderRadius: BorderRadius.circular(33), borderSide: BorderSide(color: color));
}
